# Vite Frontend Configuration
# These variables are used by Vite during frontend build and runtime

# Additional allowed hosts for the server (comma-separated)
__VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS=localhost

# Backend API URL (proxy configuration)
VITE_API_URL=/api
