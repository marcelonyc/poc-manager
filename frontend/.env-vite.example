# Base URL for nested deployments
# If deploying under a nested path (e.g., /app, /poc-manager), set this value
# Leave empty or set to / for root deployment
# Example: BASE_URL=/poc-manager
BASE_URL=/
